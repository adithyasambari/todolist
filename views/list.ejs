<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">


  <%  newListItems.forEach(function (item) {%>
    <form action="/delete" method="post">
      <div class="item">
        <input type="checkbox" name="deleteBox" value="<%=item._id%>" onChange="this.form.submit()">
        <!-- By doing .name it only will show the name of the newListItems. -->
        <p><%=  item.name  %></p>
      </div>
      <input type="hidden" name="listName" value="<%= listTitle %>" ></input>
    </form>

  <%  })%>


    <!-- for (let i=0; i<newListItems.length; i++) {dont forget to add scriplet tags
      <div class="item">
        <input type="checkbox">
        <!-- By doing .name it only will show the name of the newListItems. -->
        <!-- <p>  newListItems[i].name  </p>
      </div>   }  -->


      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value="<%=listTitle%>">+</button>
      </form>
  </div>

<%- include("footer") -%>
